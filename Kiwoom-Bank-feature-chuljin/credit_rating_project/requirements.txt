# === 필수 ===
pandas>=1.3
numpy>=1.21
scikit-learn>=1.2
matplotlib>=3.5
openpyxl>=3.1
joblib>=1.2

# === 모델 학습용 ===
catboost>=1.2.5,<2.0       # 순서형 회귀에 사용

# === 선택 패키지 (향후 활용 가능)
# seaborn>=0.11             # (선택) confusion matrix 고도화 시
# tqdm>=4.64                # (선택) 학습 진행률 표시
# fonttools>=4.28           # (선택) 한글폰트 설정 필요 시